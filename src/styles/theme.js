import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      light: "#b0bec5",
      main: "#445963",
      dark: "#1b3039",
    },
    secondary: {
      light: "#e2f1f8",
      main: "#b0bec5",
      dark: "#808e95",
    },
  },
});

export default theme;
